/dist
.DS_Store
/uploads
/node_modules
yarn-error.log

.idea

coverage

!src/**

./tsconfig.tsbuildinfo
medusa-db.sql
build
.cache

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

.medusa

# Parity snapshots (local vs Railway); regenerate with npm run parity:snapshot:local / :remote
.parity/

# Drop importer stamp files
drops/**/.imported
drops/**/.synced

# Catalog sync inbox is a desktop-sync staging area; generated drop images are tracked under drops/.
inbox/**
!inbox/
!inbox/.gitkeep
!inbox/**/
