{
  "name": "@horo/shopify-data-migration",
  "version": "1.0.0",
  "description": "HORO Shopify data migration pipeline from Medusa / web-next to Shopify Admin",
  "type": "module",
  "scripts": {
    "migrate": "tsx src/index.ts",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=20"
  }
}
