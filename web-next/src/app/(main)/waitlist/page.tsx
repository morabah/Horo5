import { PreLaunchTeaseHero } from "@/storefront/components/PreLaunchTeaseHero";

export const metadata = {
  title: "Join the Waitlist — HORO Egypt",
  description:
    "Be the first to know when HORO goes live. Original illustration by Egyptian artists, printed locally, shipped with care.",
};

export default function WaitlistPage() {
  return <PreLaunchTeaseHero />;
}
