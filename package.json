{
  "name": "horo5",
  "private": true,
  "description": "Monorepo root. Only `next` here so Vercel detects the framework and `npm ci` hoists one React tree; do not add react/react-dom here — that duplicates web-next and breaks TS/JSX for ../web imports.",
  "scripts": {
    "build": "npm run build --prefix web-next"
  },
  "dependencies": {
    "next": "16.2.3"
  },
  "devDependencies": {
    "@types/react": "^19",
    "@types/react-dom": "^19"
  }
}
