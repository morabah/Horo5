{{ 'component-horo-comparison-faq.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div
  id="ComparisonFaq-{{ section.id }}"
  class="horo-section horo-comparison-faq color-{{ section.settings.color_scheme }} gradient section-{{ section.id }}-padding{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
>
  <div class="page-width">
    {%- liquid
      assign comparison_heading = section.settings.heading
      if comparison_heading == blank
        assign comparison_heading = page.title
      endif
    -%}
    <h1 class="h0">
      {%- render 'horo-locale-or-setting', setting: comparison_heading, locale_key: 'horo.comparison_faq.heading' -%}
    </h1>

    {%- if section.settings.intro != blank -%}
      <p class="horo-comparison-faq__intro">
        {%- render 'horo-locale-or-setting', setting: section.settings.intro, locale_key: 'horo.comparison_faq.intro' -%}
      </p>
    {%- endif -%}

    {%- if section.blocks.size > 0 -%}
      <div class="horo-comparison-faq__table-wrap">
        <table class="horo-comparison-faq__table">
          <thead>
            <tr>
              <th scope="col">
                {%- render 'horo-locale-or-setting', setting: section.settings.col_buyer, locale_key: 'horo.comparison_faq.col_buyer' -%}
              </th>
              <th scope="col">
                {%- render 'horo-locale-or-setting', setting: section.settings.col_horo, locale_key: 'horo.comparison_faq.col_horo' -%}
              </th>
              <th scope="col">
                {%- render 'horo-locale-or-setting', setting: section.settings.col_proof, locale_key: 'horo.comparison_faq.col_proof' -%}
              </th>
            </tr>
          </thead>
          <tbody>
            {%- for block in section.blocks -%}
              {%- if block.type == 'comparison_row' -%}
                {%- liquid
                  assign row_prefix = 'horo.comparison_faq.rows.' | append: block.id
                  assign row_buyer_key = row_prefix | append: '.buyer'
                  assign row_horo_key = row_prefix | append: '.horo'
                  assign row_proof_key = row_prefix | append: '.proof'
                -%}
                <tr {{ block.shopify_attributes }}>
                  <th scope="row">
                    {%- render 'horo-locale-or-setting', setting: block.settings.buyer_comparison, locale_key: row_buyer_key -%}
                  </th>
                  <td>
                    {%- render 'horo-locale-or-setting', setting: block.settings.horo_answer, locale_key: row_horo_key -%}
                  </td>
                  <td class="horo-comparison-faq__proof">
                    {%- render 'horo-locale-or-setting', setting: block.settings.proof, locale_key: row_proof_key -%}
                  </td>
                </tr>
              {%- endif -%}
            {%- endfor -%}
          </tbody>
        </table>
      </div>
    {%- elsif request.design_mode -%}
      <p>Add comparison rows in the theme editor.</p>
    {%- endif -%}

    {%- if section.settings.cta_label != blank and section.settings.cta_link != blank -%}
      <div class="horo-comparison-faq__cta">
        <a href="{{ section.settings.cta_link }}" class="button">
          {%- render 'horo-locale-or-setting', setting: section.settings.cta_label, locale_key: 'horo.comparison_faq.cta_primary' -%}
        </a>
        {%- if section.settings.secondary_label != blank and section.settings.secondary_link != blank -%}
          <a href="{{ section.settings.secondary_link }}" class="button button--secondary">
            {%- render 'horo-locale-or-setting', setting: section.settings.secondary_label, locale_key: 'horo.comparison_faq.cta_secondary' -%}
          </a>
        {%- endif -%}
      </div>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "HORO Comparison FAQ",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Why HORO, not a normal printed T-shirt?"
    },
    {
      "type": "textarea",
      "id": "intro",
      "label": "Introduction",
      "default": "HORO is artist-made wearable art — not cheap print, not upload-anything custom, and not generic gift-shop merch. Here is how we compare."
    },
    {
      "type": "text",
      "id": "col_buyer",
      "label": "Column 1 header",
      "default": "You might think"
    },
    {
      "type": "text",
      "id": "col_horo",
      "label": "Column 2 header",
      "default": "Why HORO is different"
    },
    {
      "type": "text",
      "id": "col_proof",
      "label": "Column 3 header",
      "default": "Proof on site"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "Primary CTA label",
      "default": "Shop the founding drop"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "Primary CTA link"
    },
    {
      "type": "text",
      "id": "secondary_label",
      "label": "Secondary CTA label",
      "default": "Shop gifts"
    },
    {
      "type": "url",
      "id": "secondary_link",
      "label": "Secondary CTA link"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "comparison_row",
      "name": "Comparison row",
      "settings": [
        {
          "type": "text",
          "id": "buyer_comparison",
          "label": "Buyer comparison",
          "default": "Cheap printed tee"
        },
        {
          "type": "textarea",
          "id": "horo_answer",
          "label": "HORO answer",
          "default": "Artist-made, story-led, and more meaningful to wear or gift."
        },
        {
          "type": "text",
          "id": "proof",
          "label": "Proof",
          "default": "Artist card, print close-up, story on PDP"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "HORO Comparison FAQ",
      "blocks": [
        {
          "type": "comparison_row",
          "settings": {
            "buyer_comparison": "Cheap printed tee",
            "horo_answer": "Artist-made, story-led, and more meaningful to wear or gift.",
            "proof": "Artist process, print close-up, story on PDP"
          }
        },
        {
          "type": "comparison_row",
          "settings": {
            "buyer_comparison": "Custom print shop",
            "horo_answer": "Curated art with a consistent HORO world — not upload-anything printing.",
            "proof": "Artist brief, licensed artwork, cohesive collections"
          }
        },
        {
          "type": "comparison_row",
          "settings": {
            "buyer_comparison": "Fashion / hype brand",
            "horo_answer": "Less hype, more meaning — built for gifting and everyday wear.",
            "proof": "Gift route, size help, exchange policy, clean PDP proof"
          }
        },
        {
          "type": "comparison_row",
          "settings": {
            "buyer_comparison": "Generic gift shop",
            "horo_answer": "Useful like clothing, personal like a message — recipient wears the feeling.",
            "proof": "Gift hub, occasion tags, delivery + COD clarity"
          }
        },
        {
          "type": "comparison_row",
          "settings": {
            "buyer_comparison": "Marketplace seller",
            "horo_answer": "Proof, service, and trust you can verify before you buy.",
            "proof": "Reviews/UGC, WhatsApp help, Cash on delivery (COD) available"
          }
        }
      ]
    }
  ]
}
{% endschema %}
